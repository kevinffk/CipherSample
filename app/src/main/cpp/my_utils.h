#include <string.h>

void ByteToHexStr(const unsigned char* source, char* dest, int sourceLen);

void Char2Hex(unsigned char ch, char *szHex);

void CharStr2HexStr( unsigned char *pucCharStr, int iSize,  char *pszHexStr);
